//global events
Ant::init()
	...
	->bind($event, $call);

//local events
Ant::init()
	...
	->on($event, $call)
	// global events can be cancelled by
	->preventParentEvent($event);

// callback example
Ant::init()
	->get('index')
	->on('exec', function($s){
		/* some action */

		return $s;
	});


